server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://db:5432/smarthome
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect